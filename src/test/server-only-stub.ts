// Test stub for the `server-only` package, which throws when imported outside a React Server
// Component bundle. Aliased in vitest.config.ts so unit tests can import server-side modules.
export {};
